awesome — thanks for the green light. here are the minimal patch-style diffs you can drop in.

diff --git a/client/src/hooks/useBreakpoint.ts b/client/src/hooks/useBreakpoint.ts
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/client/src/hooks/useBreakpoint.ts
@@ -0,0 +1,36 @@
+// Tailwind-aligned desktop breakpoint hook (≥lg / 1024px)
+import { useEffect, useState } from "react";
+
+export function useIsDesktop() {
+  const [isDesktop, setIsDesktop] = useState<boolean>(
+    typeof window !== "undefined" && "matchMedia" in window
+      ? window.matchMedia("(min-width: 1024px)").matches
+      : false
+  );
+
+  useEffect(() => {
+    if (typeof window === "undefined" || !("matchMedia" in window)) return;
+    const mq = window.matchMedia("(min-width: 1024px)");
+    const onChange = (e: MediaQueryListEvent) => setIsDesktop(e.matches);
+    // modern browsers
+    mq.addEventListener?.("change", onChange);
+    return () => mq.removeEventListener?.("change", onChange);
+  }, []);
+
+  return isDesktop;
+}

diff --git a/client/src/state/useQuickReportStore.ts b/client/src/state/useQuickReportStore.ts
index 4ac5d77..7bc1a10 100644
--- a/client/src/state/useQuickReportStore.ts
+++ b/client/src/state/useQuickReportStore.ts
@@ -1,12 +1,51 @@
 import { create } from "zustand";
 
+type LatLng = { lat: number; lng: number };
+
 type QuickReportState = {
   sheetOpen: boolean;
   categoryId?: string;
   note: string;
-  location?: { lat: number; lng: number };
+  location?: LatLng;
+
+  // ⬇️ NEW: desktop tap-to-report support
+  reportMode: boolean;   // when true, map waits for click to set location
+  tempPin?: LatLng;      // optional pre-confirm preview
 };
 
 type QuickReportActions = {
   openSheet: () => void;
   closeSheet: () => void;
   setCategory: (id?: string) => void;
   setNote: (v: string) => void;
-  setLocation: (ll?: { lat: number; lng: number }) => void;
+  setLocation: (ll?: LatLng) => void;
+
+  // ⬇️ NEW actions for desktop flow
+  startReportMode: () => void;
+  cancelReportMode: () => void;
+  confirmTempPin: (ll: LatLng) => void;
 };
 
-export const useQuickReportStore = create<QuickReportState & QuickReportActions>((set) => ({
-  sheetOpen: false,
-  note: "",
-  openSheet: () => set({ sheetOpen: true }),
-  closeSheet: () => set({ sheetOpen: false }),
-  setCategory: (id) => set({ categoryId: id }),
-  setNote: (v) => set({ note: v }),
-  setLocation: (ll) => set({ location: ll }),
-}));
+export const useQuickReportStore = create<QuickReportState & QuickReportActions>((set) => ({
+  sheetOpen: false,
+  note: "",
+  reportMode: false,
+
+  openSheet: () => set({ sheetOpen: true }),
+  closeSheet: () => set({ sheetOpen: false }),
+  setCategory: (id) => set({ categoryId: id }),
+  setNote: (v) => set({ note: v }),
+  setLocation: (ll) => set({ location: ll }),
+
+  // desktop flow
+  startReportMode: () => set({ reportMode: true, tempPin: undefined }),
+  cancelReportMode: () => set({ reportMode: false, tempPin: undefined }),
+  confirmTempPin: (ll) =>
+    set({
+      tempPin: ll,
+      location: ll,
+      sheetOpen: true,   // open sheet after user clicks the map
+      reportMode: false,
+    }),
+}));

diff --git a/client/src/pages/community/Map.tsx b/client/src/pages/community/Map.tsx
index 1a2b3c4..5d6e7f8 100644
--- a/client/src/pages/community/Map.tsx
+++ b/client/src/pages/community/Map.tsx
@@ -1,9 +1,17 @@
 import { useEffect, useRef } from "react";
 import L from "leaflet";
 import "leaflet.markercluster";
+import { useIsDesktop } from "@/hooks/useBreakpoint";
+import { useQuickReportStore } from "@/state/useQuickReportStore";
 
 export default function Map() {
   const mapRef = useRef<L.Map | null>(null);
+  const isDesktop = useIsDesktop();
+  const {
+    reportMode,
+    confirmTempPin,
+    cancelReportMode,
+    openSheet,
+    startReportMode,
+  } = useQuickReportStore();
 
   useEffect(() => {
     // existing map init...
@@ -20,6 +28,33 @@ export default function Map() {
     return () => {
       // existing cleanup...
     };
   }, []);
 
+  // ⬇️ NEW: map click → set pin when in report mode (desktop flow)
+  useEffect(() => {
+    const map = mapRef.current;
+    if (!map) return;
+    function onClick(e: L.LeafletMouseEvent) {
+      if (!reportMode) return;
+      confirmTempPin({ lat: e.latlng.lat, lng: e.latlng.lng });
+    }
+    map.on("click", onClick);
+    return () => map.off("click", onClick);
+  }, [reportMode, confirmTempPin]);
+
+  // ⬇️ NEW: ESC to cancel report mode on desktop
+  useEffect(() => {
+    if (!isDesktop || !reportMode) return;
+    const onKey = (e: KeyboardEvent) => {
+      if (e.key === "Escape") cancelReportMode();
+    };
+    window.addEventListener("keydown", onKey);
+    return () => window.removeEventListener("keydown", onKey);
+  }, [isDesktop, reportMode, cancelReportMode]);
+
+  // ⬇️ NEW: route Report button clicks here (replace existing onClick)
+  const handleReportClick = () => {
+    if (isDesktop) startReportMode();
+    else openSheet();
+  };
+
   return (
-    <div id="map" className="h-full w-full">{/* ... */}</div>
+    <div className="relative h-full w-full">
+      <div
+        id="map"
+        className={`h-full w-full ${reportMode ? "cursor-crosshair" : ""}`}
+      />
+
+      {/* overlay helper when awaiting a click */}
+      {reportMode && (
+        <div className="pointer-events-none absolute inset-x-0 top-3 z-[500] flex justify-center">
+          <div
+            className="pointer-events-auto rounded-xl border bg-background/85 px-3 py-2 text-sm shadow"
+            aria-live="polite"
+          >
+            Click the map to set the incident location · <kbd>Esc</kbd> to cancel
+          </div>
+        </div>
+      )}
+
+      {/* ⚠️ hook this up to your existing FAB:
+          change its onClick to {handleReportClick} */}
+    </div>
   );
 }


Note for the FAB: in your existing floating “Report” button within this file, replace its onClick with onClick={handleReportClick}. If the FAB lives in its own component, pass handleReportClick down or import the store/hook there and replicate the 3-line logic.

diff --git a/client/src/features/report/QuickReportCard.tsx b/client/src/features/report/QuickReportCard.tsx
index c2f2c3a..a7b8a90 100644
--- a/client/src/features/report/QuickReportCard.tsx
+++ b/client/src/features/report/QuickReportCard.tsx
@@ -1,7 +1,11 @@
 import { Sheet, SheetContent } from "@/components/ui/sheet";
 import { Button } from "@/components/ui/button";
 import { Textarea } from "@/components/ui/textarea";
+import { useIsDesktop } from "@/hooks/useBreakpoint";
+import { useQuickReportStore } from "@/state/useQuickReportStore";
 // ... other imports
 
 export function QuickReportCard() {
+  const isDesktop = useIsDesktop();
+  const { sheetOpen, openSheet, closeSheet, location, startReportMode } = useQuickReportStore();
   // existing hooks/state/validation...
 
   return (
@@ -12,19 +16,52 @@ export function QuickReportCard() {
-      <SheetContent side="bottom" className="w-full sm:max-w-md">
+      <SheetContent side="bottom" className="w-full sm:max-w-md">
         {/* existing header / category grid / note field ... */}
 
-        {/* existing GPS button */}
-        <div className="mt-3">
-          <Button variant="secondary" onClick={getCurrentPositionAndSet}>
-            Use my current location
-          </Button>
-        </div>
+        {/* ⬇️ location row (desktop vs mobile) */}
+        <div className="mt-3 space-y-2">
+          {!isDesktop ? (
+            <Button variant="secondary" onClick={getCurrentPositionAndSet}>
+              Use my current location
+            </Button>
+          ) : (
+            <div className="flex items-center justify-between gap-3 rounded-xl border p-2">
+              <p className="text-sm text-muted-foreground" aria-live="polite">
+                {location
+                  ? `Location set: ${location.lat.toFixed(5)}, ${location.lng.toFixed(5)}`
+                  : "Click the map to set an exact spot"}
+              </p>
+              <Button variant="secondary" onClick={startReportMode}>
+                Drop a pin
+              </Button>
+            </div>
+          )}
+        </div>
 
         {/* existing photo upload / submit actions remain unchanged */}
       </SheetContent>
     </Sheet>
   );
 }

why this is safe

mobile/tablet flows are untouched; the GPS button still runs exactly as before.

on desktop, the FAB now toggles reportMode and waits for a map click; clicking sets location and opens the sheet.

no API, socket, or clustering logic changed.

tiny QA list

desktop: press Report → crosshair cursor & overlay → click map → sheet opens with coords shown.

Esc cancels report mode and hides overlay/crosshair.

mobile/tablet: Report → sheet opens immediately; GPS button works.

ping me if you want these as git apply-ready patch files or if your FAB lives outside Map.tsx and needs a micro-diff there too.