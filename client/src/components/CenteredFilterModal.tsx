import { Check } from 'lucide-react';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription } from '@/components/ui/dialog';
export interface FilterOption {value:string;label:string;icon?:React.ReactNode}
interface Props {isOpen:boolean;onClose:()=>void;title:string;options:FilterOption[];selectedValue:string;onSelect:(value:string)=>void}
export function CenteredFilterModal({isOpen,onClose,title,options,selectedValue,onSelect}:Props){return <Dialog open={isOpen} onOpenChange={open=>{if(!open)onClose()}}><DialogContent className="nn-filter-sheet"><DialogHeader><span className="nn-eyebrow">YOUR VIEW</span><DialogTitle>{title}</DialogTitle><DialogDescription>Choose what you would like to see nearby.</DialogDescription></DialogHeader><div className="nn-filter-options">{options.map(option=><button key={option.value} aria-pressed={selectedValue===option.value} onClick={()=>{onSelect(option.value);onClose()}}>{option.icon}<span>{option.label}</span>{selectedValue===option.value&&<Check size={19}/>}</button>)}</div></DialogContent></Dialog>}
